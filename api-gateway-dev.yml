spring:
  application:
    name: api-gateway
zuul:
  routes:
    myProduct:
      path: /myProduct/**
      serviceId: product
  host:
    connect-timeout-millis: 15000
    socket-timeout-millis: 60000
ribbon:
  ReadTimeout: 10000
  ConnectTimeout: 10000
server:
  port: 9092
env:
  dev
